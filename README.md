# Toto Picker
Eliminates a set of numbers and generates a new set of Toto numbers from the ones that are left.

# Initial setup
- npm create vite@latest
	- React
	- Typescript
- git config user.name, user.email
- Initial check-in
